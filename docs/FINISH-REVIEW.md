## verdict

1. Resolved — the same `docs/screenshots/fold-house-{desktop,mobile,user-509}.png` files now show “Generated concept visualization.” At 390 and 509px, the credit occupies a readable separate line below the caption; the confirmation crops verify it at actual size.
2. Resolved — the same `docs/screenshots/work-list-{desktop,mobile,user-509}.png` files now retain project number, category and concept year. Mobile confirmation crops show all fields beneath the project name with clear separation from the save control.

The refreshed full-page captures remain valid, with document tops and no missing paint regions. No material regressions from these two fixes are visible. The supplied `docs/responsive-confirmation.json` records visibility and no overflow at 360, 390, 509, 768, 1024 and 1440px, including 200% text sizing; those checks were read, not independently rerun.

## remaining

Clear. This ship verdict covers the scored fixes, not a new review of the whole surface. Complete the documentation handoff already in progress; no further visual changes are requested.

disposition: ship
